export interface Product {
  _id: string;
  name: string;
  description?: string;
  image?: string;
  category: string;
  price?: number;
  category?: string; 
  createdAt: string;
  updatedAt: string;
}
